<div class="call-to-action">
  <div class="prompt<% if (@thanks) %> hidden<% end%>">
    <h3>Want more serendipity in your life?</h3>
    <p class="lead">Let us know and we’ll add you to our invite list.</p>
    <form method="post" action="/signups" class="form-inline" >
      <input type="email" name="email" required placeholder="enter@email.com" class="input-xlarge">
      <% if (@referral_token) %><input type="hidden" name="referred_by" value="<%= @referral_token %>"><% end %>
      <input type="submit" value="Get Invited!" class="btn btn-large">
    </form>
  </div>
  <% if (@thanks) %>
  <div class="alert fade in">
    <button type="button" class="close" data-dismiss="alert">×</button>
	<h3>Thank You! Want Priority Access?</h3>
	<p class="lead">Get early access by inviting your friends. The more friends you invite, the sooner you'll get access.</p>
    <p class="lead">Just copy/paste the URL below into Twitter, Facebook or in an e-mail:</p>
    <div class="viral-url">
      <img src="/img/arrow.png" class="viral-arrow">
      <p class="viral-url text-center"><%= @viral_url %></p>
    </div>
  </div>
  <%= erb(:fb_tracking, :layout => false, :locals => { :pixel_id => settings.fb_tracking[:email_signup]} ) %>
  <% end %>        
</div>